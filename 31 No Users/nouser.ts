// Assignment 31

// Empty array of usernames
let usernames: string[] = ['admin', 'Babar Azam', 'Fakhar Zaman', 'Saim Ayub', 'Azam Khan'];

if (usernames.length === 0) {
  console.log("We need to find some users!");
} else {
  usernames = [];
    console.log('All user name has been removed.' + usernames.length)
}
